/**
 * Adapters 層 - 統一入口
 *
 * 此層包含所有跨模組適配器和外部服務適配器。
 * 用途：
 * 1. 轉換不同模組之間的介面
 * 2. 整合外部服務（支付、郵件、儲存等）
 * 3. 協調複雜的多模組交互
 *
 * 設計原則：
 * - 適配器是 DDD 架構中的一層（在 Presentation 和 Domain 之間）
 * - 所有適配器都是可選的，模組本身仍可獨立存在
 * - 適配器不應包含業務邏輯，只做轉換和協調
 * - 每個適配器都應有清晰的職責（單一責任原則）
 */

// ============ Product 相關適配器 ============
export * from './Product'

// ============ 外部服務適配器（待實作） ============
// 例：支付服務、郵件服務、儲存服務等
// export * from './External'

// ============ 複合適配器（待實作） ============
// 例：整合多個模組的複雜交互
// export * from './Integration'
